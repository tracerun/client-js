{
    "version": "0.2.0",
    "configurations": [
        {
            "type": "node",
            "request": "launch",
            "name": "Client",
            "program": "${workspaceRoot}/lib/index.js",
            "cwd": "${workspaceRoot}"
        }
    ]
}